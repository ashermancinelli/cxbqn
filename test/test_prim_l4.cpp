/*
 * This file is generated, don't edit!
 * Generated by the CXBQN build system on 2021-11-17T16:23:08
 */
#include <cxbqn/comp_utils.hpp>
#include <cxbqn/cxbqn.hpp>
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>

using namespace cxbqn;
using namespace cxbqn::types;
using namespace cxbqn::provides;

TEST_CASE("\"a\"≡⋈'a'") {
  spdlog::critical("test='{}'", "\"a\"≡⋈'a'");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("({⟨𝕩⟩}≡⋈)'a'‿2") {
  spdlog::critical("test='{}'", "({⟨𝕩⟩}≡⋈)'a'‿2");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("\"abc\"‿1≡\"abc\"⋈1") {
  spdlog::critical("test='{}'", "\"abc\"‿1≡\"abc\"⋈1");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("⋈´⊸≡\"ab\"") {
  spdlog::critical("test='{}'", "⋈´⊸≡\"ab\"");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("∧´≡⟜>¨⟨1,<'a',<∞,↕5,5‿3⥊2⟩") {
  spdlog::critical("test='{}'", "∧´≡⟜>¨⟨1,<'a',<∞,↕5,5‿3⥊2⟩");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("2‿3‿2≡≢>↕2‿3") {
  spdlog::critical("test='{}'", "2‿3‿2≡≢>↕2‿3");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("2‿3≡>⟨<2,3⟩") {
  spdlog::critical("test='{}'", "2‿3≡>⟨<2,3⟩");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("((4⥊2)⊸⥊≡(>2‿2⥊·<2‿2⥊⊢))\"abcd\"") {
  spdlog::critical("test='{}'", "((4⥊2)⊸⥊≡(>2‿2⥊·<2‿2⥊⊢))\"abcd\"");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(⊢≡>∘<)5‿3⥊↕15") {
  spdlog::critical("test='{}'", "(⊢≡>∘<)5‿3⥊↕15");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(⊢≡(><¨))5‿3⥊↕15") {
  spdlog::critical("test='{}'", "(⊢≡(><¨))5‿3⥊↕15");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(⥊≡≍)'a'") {
  spdlog::critical("test='{}'", "(⥊≡≍)'a'");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(⥊≡≍)<'a'") {
  spdlog::critical("test='{}'", "(⥊≡≍)<'a'");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(1‿2⊸⥊≡≍)\"ab\"") {
  spdlog::critical("test='{}'", "(1‿2⊸⥊≡≍)\"ab\"");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("1‿2≡1≍2") {
  spdlog::critical("test='{}'", "1‿2≡1≍2");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("2‿1(≍≡2‿2⥊∾)4‿3") {
  spdlog::critical("test='{}'", "2‿1(≍≡2‿2⥊∾)4‿3");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(≍⟜<≡≍˜)'a'") {
  spdlog::critical("test='{}'", "(≍⟜<≡≍˜)'a'");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(≍˘˜⥊˘1‿5‿9)≡⌽⎉2⊸+⥊⟜(↕×´)3‿2‿1") {
  spdlog::critical("test='{}'", "(≍˘˜⥊˘1‿5‿9)≡⌽⎉2⊸+⥊⟜(↕×´)3‿2‿1");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(<0)≡≡˘0") {
  spdlog::critical("test='{}'", "(<0)≡≡˘0");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(<1)≡≡˘<0") {
  spdlog::critical("test='{}'", "(<1)≡≡˘<0");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(2⥊<<\"ab\") ≡ ⋈˜˘<\"ab\"") {
  spdlog::critical("test='{}'", "(2⥊<<\"ab\") ≡ ⋈˜˘<\"ab\"");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(3⥊0) ≡ {-}=˘↕3") {
  spdlog::critical("test='{}'", "(3⥊0) ≡ {-}=˘↕3");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(↕4)(×⌜≡×⎉0‿2)↕5") {
  spdlog::critical("test='{}'", "(↕4)(×⌜≡×⎉0‿2)↕5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(↕4)(⋆˜⌜˜≡⋆⎉∞‿¯4)↕5") {
  spdlog::critical("test='{}'", "(↕4)(⋆˜⌜˜≡⋆⎉∞‿¯4)↕5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(⟨2⟩⊸∾⍟(2‿2⥊0‿1‿1‿1)2‿3)≡≢¨≍⎉(⌊○=)⌜˜⟨↕3,2‿3⥊↕6⟩") {
  spdlog::critical("test='{}'",
                   "(⟨2⟩⊸∾⍟(2‿2⥊0‿1‿1‿1)2‿3)≡≢¨≍⎉(⌊○=)⌜˜⟨↕3,2‿3⥊↕6⟩");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(2=⌜○↕3)≡(2‿4⥊\"abc\")≡⎉1(2‿3‿4⥊\"abc\")") {
  spdlog::critical("test='{}'", "(2=⌜○↕3)≡(2‿4⥊\"abc\")≡⎉1(2‿3‿4⥊\"abc\")");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("⟨0,0⟩≡(2‿4⥊\"abc\")≡⎉¯1(2‿3‿4⥊\"abc\")") {
  spdlog::critical("test='{}'", "⟨0,0⟩≡(2‿4⥊\"abc\")≡⎉¯1(2‿3‿4⥊\"abc\")");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(-≡-⚇¯1)5") {
  spdlog::critical("test='{}'", "(-≡-⚇¯1)5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("⟨5,⟨15,1⟩⟩≡+´⚇1⟨⟨3,2⟩,⟨⟨4,5,6⟩,⟨1⟩⟩⟩") {
  spdlog::critical("test='{}'", "⟨5,⟨15,1⟩⟩≡+´⚇1⟨⟨3,2⟩,⟨⟨4,5,6⟩,⟨1⟩⟩⟩");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("5‿6‿15≡∾´+´⚇1⟨⟨0,1⟩,⟨⟨⟩⟩⟩⥊⊸∾⚇¯2‿1⟨⟨2,3⟩,⟨4,5,6⟩⟩") {
  spdlog::critical("test='{}'",
                   "5‿6‿15≡∾´+´⚇1⟨⟨0,1⟩,⟨⟨⟩⟩⟩⥊⊸∾⚇¯2‿1⟨⟨2,3⟩,⟨4,5,6⟩⟩");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(5⥊1)≡(↕5)=○=⚇0{≍}") {
  spdlog::critical("test='{}'", "(5⥊1)≡(↕5)=○=⚇0{≍}");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("4≡2+⍟¯1 6") {
  spdlog::critical("test='{}'", "4≡2+⍟¯1 6");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(2×↕7)≡2+⍟(¯3+↕7)6") {
  spdlog::critical("test='{}'", "(2×↕7)≡2+⍟(¯3+↕7)6");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(3⌊↕5)≡{i←0⋄r←{i+↩1⋄1+𝕩}⍟(↕4)𝕩⋄r∾i}0") {
  spdlog::critical("test='{}'", "(3⌊↕5)≡{i←0⋄r←{i+↩1⋄1+𝕩}⍟(↕4)𝕩⋄r∾i}0");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(+⌜˜≡·>1+⍟⊢⊢)↕5") {
  spdlog::critical("test='{}'", "(+⌜˜≡·>1+⍟⊢⊢)↕5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("0‿1‿3‿6‿10≡+\`↕5") {
  spdlog::critical("test='{}'", "0‿1‿3‿6‿10≡+\`↕5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(-0‿1‿3‿6‿10)≡-\`↕5") {
  spdlog::critical("test='{}'", "(-0‿1‿3‿6‿10)≡-\`↕5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("((0∾¨↕3)≍3⥊0)≡≡\`↕2‿3") {
  spdlog::critical("test='{}'", "((0∾¨↕3)≍3⥊0)≡≡\`↕2‿3");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("⟨⟩≡×\`⟨⟩") {
  spdlog::critical("test='{}'", "⟨⟩≡×\`⟨⟩");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("≡⟜(!∘0\`)3‿0‿2⥊\"\"") {
  spdlog::critical("test='{}'", "≡⟜(!∘0\`)3‿0‿2⥊\"\"");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("2‿3‿5‿8‿12≡2+\`↕5") {
  spdlog::critical("test='{}'", "2‿3‿5‿8‿12≡2+\`↕5");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}

TEST_CASE("(2⋆1‿2‿6×⌜0‿2)≡3‿4⋆\`3+⌜○↕2") {
  spdlog::critical("test='{}'", "(2⋆1‿2‿6×⌜0‿2)≡3‿4⋆\`3+⌜○↕2");
  const auto rt = provides::get_runtime_cached();
  const auto runtime = rt->values;
  CompileParams p{
    Error : Couldn't read file "/home/asher/workspace/cxbqn/build-dbg/test/ccxx.bqn"
  };
  auto ret = vm::run(p.bc, p.consts.v, p.blk_defs, p.bodies);
  REQUIRE(nullptr != ret.v);
  REQUIRE(nullptr != ret.scp);
  auto n = dynamic_pointer_cast<Number>(ret.v);
  REQUIRE(nullptr != n);
  CHECK(1 == doctest::Approx(n->v));
}
